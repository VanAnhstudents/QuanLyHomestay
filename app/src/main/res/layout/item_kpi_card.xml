<?xml version="1.0" encoding="utf-8"?>
<!--
    item_kpi_card.xml â€” KPI Card (spec A6 má»¥c 3.3-3.6, ux_ui.md)
    Height = kpi_card_height (80dp), width = 0dp / weight = 1
    DÃ¹ng bÃªn trong 2 LinearLayout horizontal lá»“ng trong 1 LinearLayout vertical
    Ä‘á»ƒ táº¡o lÆ°á»›i 2Ã—2 trong fragment_home.xml.

    Java (HomeFragment.java) set runtime cho tá»«ng card:
        iv_kpi_icon  â†’ setImageResource() + setColorFilter(accent)
        tv_kpi_value â†’ setText(...)
        tv_kpi_label â†’ setText(...)
        tv_kpi_value â†’ setTextColor(accent)

    Báº£ng accent theo thá»© tá»± 4 card:
        1. Tá»•ng phÃ²ng   â†’ primary_main   #4B9DA8  ic_bed
        2. PhÃ²ng trá»‘ng  â†’ status_success #38A169  ic_home
        3. Äang thuÃª    â†’ status_warning #D97706  ic_person
        4. Doanh thu HN â†’ primary_main   #4B9DA8  ic_calendar
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:layout_weight="1"
    android:layout_margin="4dp"
    android:background="@drawable/bg_card_white"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="8dp">

    <!-- Icon KPI (24Ã—24, mÃ u accent set runtime) -->
    <ImageView
        android:id="@+id/iv_kpi_icon"
        android:layout_width="@dimen/icon_size_appbar"
        android:layout_height="@dimen/icon_size_appbar"
        android:contentDescription="KPI icon"
        android:src="@drawable/ic_home"
        app:tint="@color/primary_main" />

    <!-- GiÃ¡ trá»‹ (22sp Bold, mÃ u accent set runtime) -->
    <TextView
        android:id="@+id/tv_kpi_value"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:fontFamily="sans-serif-bold"
        android:text="0"
        android:textColor="@color/primary_main"
        android:textSize="22sp"
        android:textStyle="bold" />

    <!-- Label (11sp Caption) -->
    <TextView
        android:id="@+id/tv_kpi_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:ellipsize="end"
        android:fontFamily="sans-serif"
        android:gravity="center"
        android:maxLines="1"
        android:text="Tá»•ng phÃ²ng"
        android:textColor="@color/text_secondary"
        android:textSize="11sp" />

</LinearLayout>

